package LookForLove;

public class Female extends Person {
    public Female (Account loginInfo, Character trait, Character wish, Prompts prompt) {
        super(loginInfo, trait, wish, prompt);
    }
      
}
