package LookForLove;

public class Female extends Person {
    public Female (Account loginInfo, Character trait, Character wish, String description, Prompts[] prompt) {
        super(loginInfo, trait, wish, description, prompt);
    }

    public int totalScore(Person other) {
        if (this.wish.g
    }

    public int promptsScore(String[] other) {
    }

    public int wishScore(Person other) {
    }
      
}
