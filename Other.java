package LookForLove;

public class Other extends Person {
    public Other (Account loginInfo, Character trait, Character wish, Prompts prompt) {
        super(loginInfo, trait, wish, prompt);
    }
}
