package LookForLove;

public class Male extends Person {
    public Male (Account loginInfo, Character trait, Character wish, Prompts prompt) {
        super(loginInfo, trait, wish, prompt);
    }
    
}
